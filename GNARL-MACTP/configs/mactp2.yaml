algorithm: mactp
seed: 1
train_data:
  # graph_generator: erloop #In this case, the stay cost equals to 1
  # graph_generator: erloopmin #In this case, the stay cost equals to the min of adjacent edges
  # graph_generator: erloopzero #In this case, the stay cost of goal_nodes equals to 0s
  graph_generator: er #In this case, the stay cost of goal_nodes equals to 0s
  graph_generator_kwargs:
    p_range: [0.1, 0.2]
  node_samples:
    10: 10000
    13: 10000
    16: 10000
  num_envs: 4
  num_starts: 3    # Edit by Xiao
  num_goals: 3    # Edit by Xiao
  seed: 18
  graph_dir: graph_data_3_3
val_data:
  # graph_generator: erloop #In this case, the stay cost equals to 1
  # graph_generator: erloopmin #In this case, the stay cost equals to the min of adjacent edges
  # graph_generator: erloopzero #In this case, the stay cost of goal_nodes equals to 0s
  graph_generator: er #In this case, the stay cost of goal_nodes equals to 0s
  graph_generator_kwargs:
    p_range: [0.1, 0.2]
  node_samples:
    20: 100
  deterministic: true
  num_envs: 4
  num_starts: 3    # Edit by Xiao
  num_goals: 3    # Edit by Xiao
  seed: 32
  graph_dir: graph_data_3_3
test_data:
  # graph_generator: erloop #In this case, the stay cost equals to 1
  # graph_generator: erloopmin #In this case, the stay cost equals to the min of adjacent edges
  # graph_generator: erloopzero #In this case, the stay cost of goal_nodes equals to 0s
  graph_generator: er #In this case, the stay cost of goal_nodes equals to 0s
  graph_generator_kwargs:
    p_range: [0.1, 0.2]
  node_samples:
    16: 100
    32: 100
    # 64: 100
    # 128: 100
    # 256: 100
    # 512: 100
    # 1024: 100
  deterministic: true
  num_envs: 4
  num_starts: 3    # Edit by Xiao
  num_goals: 3    # Edit by Xiao
  seed: 47
  graph_dir: graph_data_3_3
policy_kwargs:
  embed_dim: 64
  pooling_type: max
  network_kwargs:
    network: MPNN
    aggr: max
    num_layers: 2
PPO:
  n_steps: 1024
  n_epochs: 10
  gamma: 1
  learning_rate: 0.0005
  timesteps: 50000
  batch_size: 32
  seed: 8
  eval_freq: 1024
weights_init:
  gnn_init: sparse
  encoder_init: ibarz2022
